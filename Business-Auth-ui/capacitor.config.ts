import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Business-Auth-ui',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
